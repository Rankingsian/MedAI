// Test setup for Vitest + Testing Library
import '@testing-library/jest-dom'

// Optional: polyfills or global mocks can go here
// e.g., global.fetch = global.fetch || (() => Promise.reject(new Error('fetch not implemented')))
